- DONE for each Slack presence message: login user to XMPP with
  [jid: flour_jid, nickname: slack_nickname, resource: nickname@slack]

  - DONE send corresponding messages from corresponding instances

- parse Slack messages:
  - DONE <url> -> url
  - DONE :emoticon: -> smiley/unicode emoji
  - DONE &html_entitites; -> proper chars
  - DONE <@slack id> -> username
  - DONE <#slack_id> -> channel name

- allow other messages, esp. form bots:
  - other bot messages (giphy, rightgif)
  - uri previews (? if sent by system)

- lager
  - log debug info?
 